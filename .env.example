# Copie este arquivo para um novo arquivo chamado .env e preencha os valores.
# O arquivo .env guarda dados sensiveis e nunca vai para o Git.
# As chaves abaixo serao usadas nas proximas aulas.

MONGODB_URI=
SESSION_SECRET=
PORT=3000
